import React from 'react';

/** Displays details about a company and all of its jobs */

function CompanyPage({ company, jobs }) {
  return <div>This is a company page!</div>
}

export default CompanyPage;